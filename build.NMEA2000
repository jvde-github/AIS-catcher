if [ -d "build" ]; then
  rm -rf build
  mkdir build
  echo "Build directory deleted and recreated."
else
  mkdir build
  echo "build directory created"
fi

if [ -d "NMEA2000_AC" ]; then
  rm -rf NMEA2000_AC
  echo "NMEA2000 library directory deleted."
fi

git clone https://github.com/jvde-github/NMEA2000_AC.git
cd NMEA2000_AC/
scripts/cmake.sh 
cd ..

cd build; cmake .. -DNMEA2000_PATH=..; make; sudo make install; cd ..
